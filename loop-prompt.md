Implement the next task from browser-testing.md. One task per loop:

1. Read **browser-testing.md**. Pick the **first** unchecked `- [ ]` in Task Board order. Ordering rules (from that file's "How to use" + phase notes): Phases 1–3 are strictly sequential (the harness); any Phase-4 task is fair game once Phase 3 is green; Phase 5 is last. Skip tasks marked **(optional)** unless nothing else remains, and skip any marked `⛔ blocked`. If no eligible task is left, output `LOOP-DONE` on its own line as your entire reply and stop. Otherwise **never** write that token anywhere — not even to say you are *not* done.
2. Do **only that task**. Follow its `## Tnn` section (Goal / Steps / Files / Acceptance) and the project conventions in **CLAUDE.md** (namespace `Reach\StatamicResrv\`, PHP 8.4 explicit types + PHPDoc, PHPUnit + Orchestra Testbench, cross-driver SQL for SQLite/MySQL/Postgres, money in integer cents, traits over inheritance). The **"⚠️ Gotchas reference"** section of browser-testing.md is load-bearing — consult it when wiring the harness.
3. **Verify — the task's own `Acceptance` check is the gate and must pass.** Additionally: `vendor/bin/pint --dirty` must be clean for any PHP you touched, and if you changed addon source under `src/`, keep the existing suite green (`composer test`; the full run has known order-dependent flakes, so a targeted `vendor/bin/phpunit tests/<Dir>` is fine when that's what your change touches). Fix until green.
4. Flip that task's `- [ ]` → `- [x]` in browser-testing.md, bump the `**Progress: X / 23**` counter, then stage new **and** modified files and commit: `git add -A && git commit -m "<Tnn>: <title>"` (plain `git commit -am` skips newly created files like migrations/tests).
5. **Stop** — don't start another task.

If a task **cannot be completed headlessly** — it needs a manual eyes-on check (e.g. **T9**), an interactive login, or a tool/dependency that isn't installed and that you must not install unattended — append `⛔ blocked: <why>` under that task in browser-testing.md, leave it unchecked, and pick the next eligible task instead.
